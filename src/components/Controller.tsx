import React from "react";
import { Vector3 } from "three";

// class Controller extends React.Component {}
// const Controller = React.forwardRef(({...props}, ref) => {return () => {console.log("A")}};

// export { Controller };

/*
 * Controller
 * @param landmarks
 *
 * Controll child components
 *
 *  .normalToVector3(x: number, y: number, z: number): Vector3
 *  .normalToWorld(x: number, y: number, z: number, viewport: Size): Vector3
 *  .setPosition(x: number, y: number, z: number, viewport: Size): void? Ref?
 *
 * Example: BOTU!!
 *  <Controller>
 *      <SOMEOBJECT | GROUP />
 *  </Controller>
 *
 */
